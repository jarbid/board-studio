{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Default capability for the main Board Studio window.",
  "windows": ["main"],
  "permissions": ["core:default"]
}
